
const QuestionCard =({
    title,
    description,
}) => {
    return (
        <div>
            <h2>{title}</h2>
            <h2>{description}</h2>
        </div>
    )
}

export default QuestionCard